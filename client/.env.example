# Client environment — copy to .env and fill in (Vite bakes these at build time,
# so rebuild after changing them).
# Production API base URL (e.g. https://vocab-vault-api.onrender.com/api).
# Leave unset locally: the Vite dev proxy forwards /api to localhost:5000.
VITE_API_URL=
